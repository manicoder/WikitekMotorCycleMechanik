<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WikitekMotorCycleMechanik.Views.TestServerPage"
             BackgroundColor="{StaticResource page_bg_color}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Example Item"
                     Order="Primary"
                     Priority="0"
                     Clicked="ToolbarItem_Clicked">
            <ToolbarItem.IconImageSource>
                <FontImageSource 
                    FontFamily="{StaticResource Material}"
                    Glyph="&#xF0111;" 
                    Color="{StaticResource theme_color}"
                    Size="40"/>
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout HorizontalOptions="CenterAndExpand" 
                     VerticalOptions="CenterAndExpand">
            <Label Text="This is test server application."
                   FontSize="18"
                   VerticalOptions="CenterAndExpand" 
                   HorizontalOptions="CenterAndExpand"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>